<!-- keys/gate.md -->

# gate

**Reference URL:** `cgp:/r/keys/gate.md`

The decision gate within the matched frame that contains the trigger. The `value` for this row resolves to a `cgp:/r/gates/<gate-name>.md` reference.

## Example row

| key | value |
|---|---|
| `cgp:/r/keys/gate.md` | `cgp:/r/gates/act.md` |
