{
  "name": "cgp",
  "version": "0.1.0",
  "description": "Context Graph Protocol — reference implementation",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "for f in tests/*.test.mjs; do node \"$f\" || exit 1; done"
  },
  "dependencies": {
    "ws": "^8.0.0",
    "ajv": "^8.0.0",
    "ajv-formats": "^3.0.0"
  }
}