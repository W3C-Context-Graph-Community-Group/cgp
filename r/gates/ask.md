<!-- gates/ask.md -->

# ask

**Reference URL:** `cgp:/r/gates/ask.md`

Decision gate that pauses and requests input from the user. When a trigger under `ask` fires, the boundary crossing is held pending user response.

One of three decision gates. See also: `cgp:/r/gates/halt.md`, `cgp:/r/gates/act.md`.
