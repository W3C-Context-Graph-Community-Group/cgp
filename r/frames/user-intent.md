<!-- frames/user-intent.md -->

# user-intent

**Reference URL:** `cgp:/r/frames/user-intent.md`

The user's section of the intent map. User responses, user-set policies, user-declared intents during the session — what should happen so that the user is well-served.

One of three frames in every intent map. See also: `cgp:/r/frames/program-intent.md`, `cgp:/r/frames/business-intent.md`.
